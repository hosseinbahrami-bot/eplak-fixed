# راهنمای نصب و راه‌اندازی پروژه ای‌پلاک

این راهنما برای نصب و اجرای پروژه بصورت کامل در محیط لوکال (Localhost) نوشته شده است. هدف این راهنما این است که شما بتوانید بدون دردسر، پروژه را روی سیستم خود راه‌اندازی کنید و از بخش‌های شهروندی و پنل ادمین استفاده کنید.

---

## 1. پیش‌نیازها

قبل از شروع، باید این موارد روی سیستم شما نصب باشند:

- PHP 7.4 یا بالاتر (با اکستنشن‌های `pdo_mysql` یا `pdo_sqlite`)
- **یا** MySQL/MariaDB **یا** حالت SQLite (توضیح در بخش بعد — هیچ‌کدام از سرورها لازم نیست)
- Apache یا Nginx، یا همان سرور داخلی PHP (`php -S`)
- phpMyAdmin (اختیاری ولی خیلی مفید است)
- Composer (اختیاری برای آینده)

اگر از XAMPP استفاده می‌کنید، معمولاً همه این موارد را به‌صورت آماده دارد.

---

## 2. دانلود یا کپی پروژه

پروژه را در مسیر مناسب قرار دهید. اگر از XAMPP، معمولاً مسیر زیر است:

- Windows: C:\xampp\htdocs\eplak-fixed

اگر پروژه را در مسیر دیگری کپی کرده‌اید، مسیر وب‌سرور باید به آن اشاره کند.

مثال:
- http://localhost/eplak-fixed/

---

## 2.5 ⚡ راه‌اندازی سریع بدون MySQL و بدون XAMPP (حالت SQLite)

اگر سرور MySQL/MariaDB ندارید (مثلاً فقط PHP روی سیستم یا سرور شماست)، می‌توانید
بدون هیچ سرور دیتابیس پروژه را اجرا کنید؛ دیتابیس یک فایل SQLite ساده در
پوشه‌ی `data/` است و همه‌چیز (جداول + ادمین پیش‌فرض) خودکار ساخته می‌شود:

1. فایل تنظیمات را بسازید:

```bash
cp shared/config.example.php shared/config.php
```

و آن را به این شکل تنظیم کنید (یا از متغیر محیطی `DB_DRIVER=sqlite` استفاده کنید):

```php
return [
    'driver' => 'sqlite',
];
```

2. وب‌سرور داخلی PHP را اجرا کنید:

```bash
php -S 0.0.0.0:8080
```

3. آدرس‌ها:

- اپ شهروندی: `http://localhost:8080/`
- پنل ادمین: `http://localhost:8080/admin/login.php`
- ورود پیش‌فرض: `admin` / `admin123`

> این حالت برای توسعه، دمو و پیش‌نمایش است. برای محیط تولید همان حالت MySQL
> (بخش‌های بعد) توصیه می‌شود.

---

## 3. ساخت دیتابیس MySQL

1. وارد phpMyAdmin شوید یا از خط فرمان به MySQL وصل شوید.
2. یک **دیتابیس خالی** با نام دلخواه ایجاد کنید:

```sql
CREATE DATABASE eplak_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

> جداول (users، reports، notifications، tickets، departments، admin_users و…)
> **به‌صورت خودکار** در اولین اجرا ساخته می‌شوند، بنابراین نیازی به ایمپورت
> دستی اسکیمای SQL نیست. اگر بخواهید از داده‌های نمونه‌ی موجود استفاده کنید،
> می‌توانید [backend/eplak_db.sql](backend/eplak_db.sql) را ایمپورت کنید
> (در این صورت نام دیتابیس باید دقیقاً با نامی که در آن فایل dump شده یکی باشد).

---

## 4. تنظیم اتصال دیتابیس

اطلاعات اتصال از این دو منبع خوانده می‌شود (به این ترتیب اولویت):

1. متغیرهای محیطی: `DB_HOST`، `DB_USER`، `DB_PASS`، `DB_NAME`
2. فایل [shared/config.php](shared/config.example.php) — این فایل **در گیت
   قرار نمی‌گیرد**، پس باید خودتان بسازید:

```bash
cp shared/config.example.php shared/config.php
```

سپس مقادیر را مطابق دیتابیس خود تنظیم کنید:

```php
return [
    'host' => '127.0.0.1',
    'user' => 'root',
    'pass' => '',
    'name' => 'eplak_db',
];
```

> ⚠️ بدون این فایل، کد به‌صورت پیش‌فرض با کاربر `wigitali_root` و دیتابیس
> `wigitali_eplak-db` تلاش می‌کند که روی نصب‌های تازه وجود ندارند؛ همین باعث
> می‌شود صفحه ادمین باز نشود (به‌جای صفحه سفید، اکنون صفحه‌ی خطای خوانا با
> فهرست دلایل نمایش داده می‌شود).

اگر از XAMPP، معمولاً `root` بدون رمز روی `127.0.0.1` کار می‌کند.

---

## 5. راه‌اندازی پروژه

### 5.1 اجرای اپ اصلی

پس از قرار دادن پروژه در فولدر وب‌سرور، این آدرس را باز کنید:

```text
http://localhost/eplak-fixed/
```

این آدرس، اپ اصلی شهروندی را لود می‌کند.

### 5.2 اجرای پنل ادمین

برای ورود به پنل مدیریت، این آدرس را باز کنید:

```text
http://localhost/eplak-fixed/admin/login.php
```

### 5.3 ورود به پنل ادمین

کاربر پیش‌فرض ادمین:

- نام کاربری: admin
- رمز عبور: admin123

این کاربر در دیتابیس به‌صورت خودکار در جدول admin_users ایجاد می‌شود در صورت خالی بودن جدول.

---

## 6. ساختار پروژه

پروژه به چند بخش اصلی تقسیم شده است:

- [index.html](index.html) – صفحه اصلی اپ شهروندی
- [app.js](app.js) – نقطه ورود جاوااسکریپت
- [core](core) – منطق اصلی و state/storage
- [modules](modules) – ماژول‌های ورود، گزارش، پروفایل و داشبورد
- [admin](admin) – پنل مدیریتی
- [backend](backend) – سرویس و اسکیمای دیتابیس
- [shared](shared) – لایه مشترک برای دیتابیس و session

---

## 7. امکانات فعلی پروژه

### بخش شهروندی
- ورود شماره موبایل
- ثبت گزارش و تیکت
- مشاهده گزارش‌ها
- پروفایل کاربر
- پرداخت و اعلان‌ها

### پنل ادمین
- داشبورد مدیریت
- مشاهده گزارش‌ها
- مشاهده کاربران
- مدیریت تیکت‌ها
- فیلتر و جست‌وجو
- پاسخ به تیکت‌ها
- حذف گزارش/کاربر/تیکت

---

## 8. نکات مهم برای اجرای درست

- مطمئن شوید Apache و MySQL روشن باشند.
- دیتابیس eplak_db باید وجود داشته باشد.
- فایل‌های PHP باید با کدگذاری UTF-8 باشند.
- اگر خطایی در اتصال دیتابیس دیدید، تنظیمات user/password/host را بررسی کنید.
- اگر صفحه سفید می‌بینید، خطای PHP را از لاگ سرور بررسی کنید.

---

## 9. اگر خطا داشتید

### صفحه ادمین باز نمی‌شود (خطای «اتصال به دیتابیس برقرار نشد»)
همه‌ی صفحه‌های ادمین — حتی صفحه لاگین — در همان ابتدا به MySQL وصل می‌شوند؛
اگر اتصال شکست بخورد، به‌جای صفحه سفید، صفحه‌ی خطای خوانا نمایش داده می‌شود
که پیام دقیق خطا (مثلاً Connection refused یا Access denied) را نشان می‌دهد.
بررسی کنید:
- MySQL روشن باشد
- دیتابیس موجود در `shared/config.php` وجود داشته باشد
- نام کاربری، رمز و هاست درست باشند
- اکستنشن `pdo_mysql` در PHP فعال باشد

### خطای اتصال به دیتابیس
بررسی کنید:
- MySQL روشن باشد
- دیتابیس eplak_db وجود داشته باشد
- نام کاربری و رمز عبور درست باشند

### خطای 404
بررسی کنید:
- پوشه پروژه درست در htdocs قرار گرفته باشد
- آدرس درست باشد

### خطای صفحه سفید
این معمولاً به دلیل خطای PHP است. در XAMPP، لاگ خطاها را در این مسیر ببینید:

- C:\xampp\php\logs\php_error_log

---

## 10. پیشنهاد برای توسعه آینده

پروژه برای توسعه‌ی بیشتر آماده است. در آینده می‌توانید این موارد را اضافه کنید:

- احراز هویت قوی‌تر با session امن‌تر
- نقش‌های چندگانه برای مدیران
- اتصال مستقیم اپ به API
- فایل پیوست برای تیکت‌ها
- داشبورد آماری پیشرفته
- ارسال اعلان به کاربر

---

## 11. جمع‌بندی

**راه‌اندازی سریع (بدون MySQL):**
1. `cp shared/config.example.php shared/config.php` و در آن بنویسید: `'driver' => 'sqlite'`
2. `php -S 0.0.0.0:8080`
3. پنل ادمین: `http://localhost:8080/admin/login.php` (ورود: admin / admin123)

**راه‌اندازی کامل (MySQL):**
1. XAMPP یا سرور محلی را روشن کنید.
2. یک دیتابیس خالی (مثلاً eplak_db) ایجاد کنید — جداول خودکار ساخته می‌شوند.
3. `shared/config.php` را با اطلاعات دیتابیس بسازید.
4. پروژه را در htdocs قرار دهید.
5. به آدرس localhost/eplak-fixed/ بروید.
6. برای پنل مدیریتی به admin/login.php بروید.

اگر بخواهید، در مرحله بعد می‌توانم برای شما یک نسخه خیلی حرفه‌ای‌تر از این مستند هم آماده کنم که شامل:
- تصویری کردن ساختار پوشه‌ها
- آموزش نصب روی XAMPP
- آموزش تست API
- و راهنمای کار با پنل ادمین
هم باشد.
